# Ralph Progress

Project: Living Stories Collective Website
Branch: ralph/living-stories-collective-landing-page
Source PRD: tasks/prd-living-stories-collective-landing-page.md
Paper Design File:
 https://app.paper.design/file/01KM34BNDFPQATR9E3R3411Z88?node=1-0
 https://app.paper.design/file/01KM34BNDFPQATR9E3R3411Z88?node=34X-0
PRD JSON: tasks/prd.json
Created: 2026-03-20

## Status

Current story: US-007 (Instagram profile embed in hero)
Completed: 6/13

## Ralph Instructions

- The Paper design is the source of truth for all remaining UI work.
- Use Paper MCP to inspect the Paper design file and artboards.
- Do not use Playwright or the paper.design web app as the primary design-reference path.
- Use browser tooling only for verifying the implemented local site.
- Before marking a UI story complete, compare the implemented section against both Paper artboards:
  - Desktop: https://app.paper.design/file/01KM34BNDFPQATR9E3R3411Z88?node=1-0
  - Mobile: https://app.paper.design/file/01KM34BNDFPQATR9E3R3411Z88?node=34X-0
- Do not treat functional completion as story completion if the design visibly diverges from Paper.
- Match layout structure, spacing, copy hierarchy, imagery, and shader/background effects as closely as possible.
- If a story is blocked on design fidelity, record the exact mismatch here before moving to the next story.

## Story Queue

- [x] US-001: Create landing page shell and thin index route
- [x] US-002: Add reusable Paper shader background layer
- [x] US-003: Update public branding and homepage metadata
- [x] US-004: Build header navigation component
- [x] US-005: Build hero media and copy section
- [x] US-006: Align completed shell, header, shader, and hero with Paper using Paper MCP
- [ ] US-007: Add Instagram profile embed to the hero
- [ ] US-008: Build the upcoming sessions section with Luma
- [ ] US-009: Render the How It Works section from structured step data
- [ ] US-010: Add the feedback quotes section
- [ ] US-011: Add the feedback stats snapshot
- [ ] US-012: Build the Get Involved action cards
- [ ] US-013: Add the connect block and footer

## Notes

- Implemented the `US-001` route refactor in `src/pages/index.astro`, `src/components/landing/LandingPage.astro`, `src/layouts/LandingPageShell.astro`, and `src/styles/landing-page.css`.
- The old swipe container, progress bar markup, and homepage scroll script were removed from `src/pages/index.astro`.
- Implemented `US-002` by adding the pinned `@paper-design/shaders-react` dependency, the reusable `src/components/landing/LandingShaderBackground.tsx` island, and the centralized `src/components/landing/shaderBackgroundConfig.ts` tuning module.
- Mounted the shader background from `src/layouts/LandingPageShell.astro` and updated `src/styles/landing-page.css` so the `PaperTexture` layer renders at `opacity: 0.26` with `mix-blend-mode: multiply` and the `SimplexNoise` layer uses `mix-blend-mode: screen`.
- Implemented `US-003` by centralizing the homepage brand text, description, register page title, and Instagram URL in `src/utils/site.ts`.
- Updated the landing-page metadata and eyebrow branding to use `Living Stories Collective`, and added `og:site_name` in `src/layouts/LandingPageShell.astro`.
- Updated the `/register` document title to `Register — Living Stories Collective`.
- `npm ci` completed successfully in this worktree to install the pinned dependencies.
- Cleared the pre-existing repo-wide validation blockers by removing merge conflict markers from `src/pages/book-cover-generator.astro` and fixing the `setTimeout` typing in `src/pages/reader/[reader].astro`.
- `npm run astro check` now passes for the repo.
- Browser verification succeeded for `http://127.0.0.1:4321/` using `agent-browser` and Playwright; the landing page rendered and the shader layers were present with the expected blend modes.
- Browser verification succeeded for `http://127.0.0.1:4321/` and `http://127.0.0.1:4321/register`; the homepage title, Open Graph tags, Twitter tags, and register page title all resolved to `Living Stories Collective`.
- `npm run build` also passes for the repo.
- Implemented `US-004` with the reusable `src/components/landing/LandingHeader.astro` component and a named header slot in `src/layouts/LandingPageShell.astro` so the navigation renders as a full-width bar above the landing-page content.
- Updated `src/components/landing/LandingPage.astro` and `src/styles/landing-page.css` with the Paper-inspired logo treatment, responsive desktop/mobile header layout, and stable same-page anchors for `#how-it-works`, `#get-involved`, and `#contact`.
- `npm run astro check` passes, and browser verification confirmed the header renders correctly on desktop and mobile while the nav links resolve to the expected in-page anchors.
- Implemented `US-005` with the new `src/components/landing/LandingHero.astro` section component, replacing the placeholder homepage hero in `src/components/landing/LandingPage.astro`.
- Updated `src/styles/landing-page.css` with the hero-specific two-column panel, editorial CTA styling, paired desktop media treatment, and stacked mobile media layout.
- The hero now uses the local `public/images/mcr-hrd-book-10-reading.png` and `public/images/mcr-hrd-book-13-reading.png` assets and links the CTA to `#upcoming-sessions`.
- `npm run astro check` and `npm run build` both pass after the hero changes.
- Browser verification at `http://127.0.0.1:4321/` confirmed the hero renders correctly on desktop and mobile, and the `See upcoming sessions` CTA updates the URL to `#upcoming-sessions`.
- Tightened Ralph execution guidance so remaining UI stories must match the Paper artboards before they can be marked complete.
- Added a new immediate Ralph task to reconcile the already completed shell/header/shader/hero work against the Paper artboards before new sections are added.
- Tightened the Ralph runner instructions so Paper design inspection must go through Paper MCP rather than the Paper web app in Playwright.
- Implemented `US-006` by inspecting the Paper desktop (`1-0`) and mobile (`34X-0`) artboards with Paper MCP and aligning the shell, header, shader presentation, and hero slice to those measurements before moving on.
- Updated `src/components/landing/LandingHeader.astro`, `src/components/landing/LandingHero.astro`, `src/components/landing/LandingPage.astro`, `src/layouts/LandingPageShell.astro`, and `src/styles/landing-page.css` to match the Paper header spacing, typography, logo treatment, hero proportions, image ordering, and CTA stack on desktop and mobile.
- Removed the non-Paper scaffold cards from the homepage so the current implementation only renders the completed slice plus invisible anchor targets for later same-page sections.
- Added a static Instagram preview placeholder block sized to the Paper hero so the layout matches the artboard now; the live `@livingstoriesco` embed itself remains the next story in `US-007`.
- Paper-vs-local verification showed the key desktop and mobile slice measurements now line up closely, including the 1440x109 desktop header, 390x143 mobile header, 1188px desktop hero width, 358px mobile hero width, the 360x471 desktop preview card, and the 322x471 mobile preview card.
- `npm run astro -- check` passes with existing repo hints only, and `npm run build` passes.
- Browser verification at `http://127.0.0.1:4321/` confirmed the revised header and hero render in the Paper-like desktop and mobile structure, and the CTA plus same-page anchor targets still resolve correctly.
